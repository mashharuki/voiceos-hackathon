{
  "name": "voiceos-hackathon",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "build": "pnpm -r build",
    "dev:backend": "pnpm --filter backend dev",
    "dev:mcp": "pnpm --filter mcp-server dev",
    "start:mcp": "pnpm --filter mcp-server start:tsx",
    "lint": "biome check .",
    "format": "biome format --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4"
  }
}
